package checker;

import manager.Settings;

class HorizontalChecker extends CheckerCenter {
	
	public HorizontalChecker(Settings settings) {
		super(settings);
	}
	{
	addRow = 0;
	addCol = 1;
	}

}
